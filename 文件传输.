在线文件传输， 服务器实时转发以及断点续传

离线文件传输，传输到服务器指定目录，当用户请求下载时，从服务器下载。


断点续传原理：
（在线）：
	发送端记录当前传送的字节数
	服务器记录转发的字节数
	接受端记录接收的字节数。

	当发送端意外终止时，以接受端保存的记录为准
 （离线）：
	当发送端或者服务器异常终止（手动终止）， 以服务器保存的记录为准
	当接受短或者服务器异常终止（手动终止）， 以接受端保存的记录为准
 
